package Tree.Attribute;

import Tree.Node;

public class Lc559 {
    /*

    //后续遍历： 计算树的高度
    //1. 确定递归函数的参数和返回值：参数是传入树的根结点，返回值是树的深度
    //2. 确定终止条件: 如果节点为null return 0
    //3. 确定单层递归逻辑：先求左子树深度，再求右子树深度，最后求左右深度最大值再+1
    public int maxDepth(Node root){
        if(root==null) return 0;
        int depth = 0;
        for(Node node:root.children){
            depth = Math.max(depth,maxDepth(node));
        }
        return depth+1;
    }

    //前序遍历： 计算树的深度
    int res = 0;
    public int maxDepth(Node root){
        //res = Math.max(res,)
    }
    public

     */
}
